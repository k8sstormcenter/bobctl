# AppArmor profile for replicaset-harbor-nginx
profile replicaset-harbor-nginx-profile flags=(attach_disconnected,mediate_deleted) {
  #include <tunables/global>
  # Executable: /usr/sbin/nginx
  /usr/sbin/nginx \{
    capability DAC_OVERRIDE,
    capability DAC_READ_SEARCH,
  }
}
